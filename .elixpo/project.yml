project:
  name: sketch.elixpo
  description: "LixSketch — Whiteboard/Canvas (monorepo)"
  stack: nextjs
  node_version: "20"

ci:
  lint_cmd: ""
  test_cmd: ""
  build_cmd: "npm run build"

maintainers:
  - github: Circuit-Overtime
    skills: [frontend, nextjs, ui, cloudflare]
    lead: true

gist:
  id: ""
  account: elixpoo
